# Clerk Authentication
# Get your SECRET key from https://clerk.com dashboard
# This is the SECRET key (starts with sk_test_ or sk_live_)
# IMPORTANT: Keep this secret! Never commit it to git or expose it in frontend!
CLERK_SECRET_KEY=sk_test_your_secret_key_here

# Clerk Issuer URL (optional, for JWT verification)
# Format: https://your-app.clerk.accounts.dev
CLERK_ISSUER_URL=https://your-app.clerk.accounts.dev

# PostgreSQL Database Connection
# Format: postgresql://username:password@host:port/database
# Examples:
# Local: postgresql://postgres:password@localhost:5432/winedb
# Neon: postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/neondb
# Supabase: postgresql://postgres:password@db.xxx.supabase.co:5432/postgres
DATABASE_URL=postgresql://user:password@host:5432/winedb

# Server Port
PORT=4000

# Frontend Origin (for CORS)
# For local development only: http://localhost:3000
# For production only: https://chronictrade-frontend.onrender.com
# For both (recommended): http://localhost:3000,https://chronictrade-frontend.onrender.com
FRONTEND_ORIGIN=http://localhost:3000,https://chronictrade-frontend.onrender.com

# Environment
# Set to "production" for cloud deployments (enables auto-migrations)
ENV=production

# JWT Secret (for custom JWT if needed)
# Generate a random string: openssl rand -base64 32
JWT_SECRET=your-random-secret-key-here

